spring:
  jpa:
    hibernate:
      ddl-auto: none
      properties:
        hibernate:
          format_sql: true
          show-sql: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/artist
    username: root
    password: 74320305
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB
  logging:
    level:
      org.hibernate: info
      org.hibernate.SQL: debug
      org.hibernate.type: trace